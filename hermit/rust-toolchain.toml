# Pin the toolchain: the codebase uses edition-2024 features (let-chains) and
# panic=abort release semantics that deserve a known-good compiler. Keep in
# sync with the version CI uses (.github/workflows/ci.yml).
[toolchain]
channel = "1.97"
components = ["rustfmt", "clippy"]
